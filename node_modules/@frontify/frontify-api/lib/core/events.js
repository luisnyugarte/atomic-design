var EventEmitter = require('events');
var logger = require('./logger');

var events = new EventEmitter();

events.on('log', function(type, message) {
  if(logger[type]) {
    logger[type](message);
  }
});

module.exports = events;